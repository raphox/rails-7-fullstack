<%= turbo_stream.remove "list_#{dom_id(@kit_product)}" %>
<%= turbo_stream.update "#{@kit_product.model_name.singular}_form", partial: "kit/products/product", locals: { flash: flash, product: Kit::Product.new } %>

